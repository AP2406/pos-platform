# Release proof — marketing site at 056e35e (JPEG q45; 2560 set resampled to 1600 px wide)

Full-page captures of every marketing route at 375, 1280, 1728 and 2560 px,
taken from the production build of the tree that ships the mockup-exact home
page and its chrome site-wide (Inter, Product · Solutions · Pricing · Guides,
four-column footer). Captured 15 Sep 2026 against `next start` on the merged
tree; the marketing files are byte-identical between 67f0806 and 056e35e.

Checks that passed on the same server (scripts: see /tmp/surge-check.mjs at
capture time): 22 routes 200; home H1 "Run the rush. Grow your business.";
one header/footer signature across all 22 routes; the ten legacy URLs 308 to
their PAGE-MAP targets; the mandated terminal caption on every page that shows
the terminal; the Sri Lanka pilot form present with no forbidden claims.
