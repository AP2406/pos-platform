{
  "permissions": {
    "defaultMode": "acceptEdits",
    "allow": ["Bash(*)"],
    "deny": [
      "Read(./.env.local)",
      "Edit(./.env.local)",
      "Bash(git push --force *)",
      "Bash(git push * --force)"
    ]
  }
}
