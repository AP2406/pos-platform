{
  "name": "@surge/api-contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared request/response types for the Surge v1 HTTP API (§2 of the Native iOS POS blueprint). Type-only — imported by the web Route Handlers and the RN client so the contract has one source of truth.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false
}
